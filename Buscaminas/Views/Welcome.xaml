<UserControl x:Class="Buscaminas.Views.Welcome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Buscaminas.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Focusable="True">

    <UserControl.InputBindings>

        <MouseBinding Gesture="LeftClick" Command="{Binding StartCommand}"/>

    </UserControl.InputBindings>


    <Grid>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <Label Content="BUSCAMINAS"
                       FontSize="72"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>

                <TextBlock Text="— Presiona AQUÍ para comenzar —"
                       FontSize="14"
                       FontStyle="Italic"
                       HorizontalAlignment="Center"
                       Margin="0,24,0,0">
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0.2" To="1"
                                                 Duration="0:0:1.2"
                                                 AutoReverse="True"
                                                 RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>

            </StackPanel>

        </Grid>
        
</UserControl>
