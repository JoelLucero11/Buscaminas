<UserControl x:Class="Buscaminas.Views.Game"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Buscaminas.Views" 
             xmlns:viewmodels="clr-namespace:Buscaminas.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel HorizontalAlignment="Center" Grid.Column="0">
            
            <TextBlock Text="Buscaminas" 
                       HorizontalAlignment="Center"/>
            
            <Button Content="Reiniciar"
                    Width="150" 
                    Height="40"
                    Command="{Binding RestartCommand}"/>
            
            <Button Content="Configuración" Width="150" Height="40"/>
            
            <Button Content="Abandonar"
                    Width="150" 
                    Height="40"
                    Command="{Binding BackCommand}"/>

            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="30"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,100,0,0"/>

        </StackPanel>

        <StackPanel Grid.Column="1" >
            <TextBlock Text="{Binding ElapsedTime}" 
                       FontSize="40" 
                       HorizontalAlignment="Center"/>
            
            <ItemsControl ItemsSource="{Binding Cells}"
                          Height="410" 
                          Width="410" 
                          HorizontalAlignment="Center">
            
                <ItemsControl.ItemsPanel>
                
                    <ItemsPanelTemplate>
                         <UniformGrid Columns="{Binding Column}"/>
                    </ItemsPanelTemplate>
                
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource CellButton}"
                                Command="{Binding DataContext.RevealCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}">
                            
                            <Button.InputBindings>
                                
                                <MouseBinding MouseAction="RightClick"
                                              Command="{Binding DataContext.FlagCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}" />
                                
                            </Button.InputBindings>
                            
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </StackPanel>

    </Grid>
</UserControl>
